<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema Splash Screen -->
    <style name="Theme.WhereIsItProject" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/designer</item>
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.WhereIsItProject.App</item>
    </style>

    <!-- Tema real de la app, sin barra arriba -->
    <style name="Theme.WhereIsItProject.App" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

</resources>